"use client";
import { Screen, Header } from "@/components/ds";

/** STUB: replaced by a screen implementation. */
export default function SkillCheckPage() {
  return (
    <Screen>
      <Header />
      <div style={{ padding: "36px 38px", color: "var(--kc-ink-dim)" }}>Skill check · coming next.</div>
    </Screen>
  );
}
